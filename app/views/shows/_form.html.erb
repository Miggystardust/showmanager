<script>

$(function() {
  $( ".datepicker" ).datetimepicker({
    showButtonPanel: true,
    dateFormat: "yy-mm-dd",
    timeFormat: "hh:mm"
  });
});
</script>
<div class="well">
<%= form_for @show, :html => { :class => "form-horizontal" } do |f| %>
 <fieldset>
 <% if @show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@show.errors.count, "error") %> prohibited this show from being saved:</h2>

      <ul>
      <% @show.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% logger.debug ("did show errors") 
     end %>

  <div class="form-group">
    <%= f.label :troupe_id, "Troupe", :class=>"col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%=select_tag "troupe_id", options_from_collection_for_select(@troupesel, "id", "name", @show.troupe_id), prompt: "-- select a troupe --" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :title, "Title", :class=>"col-sm-2 control-label" %>
    <div class="col-sm-10">
    <%= f.text_field :title, :class=>"form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :venue, "Venue", :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
     <%= f.text_field :venue, :class => "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :door_time, "Door Time", :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
    <%= f.text_field :door_time, :class => "form-control datepicker" %>
    </div>
  </div>


  <div class="form-group">
    <%= f.label :show_time, "Show Time", :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
    <%= f.text_field :show_time, :class => "form-control datepicker" %>
    </div>
  </div>
<BR>
    <%= link_to 'Cancel', shows_path, :class => "btn btn-danger" %>
    <%= f.submit "Save Details", :class => "btn btn-primary" %>


</fieldset>
<% end %>
</div>
