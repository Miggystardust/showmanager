<h2>Sign in</h2>

<div id="stylized" class="myform">
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class => "form-horizontal" } ) do |f| %>
  <fieldset>
<div class="control-group">
 <div class="controls">
  <P>
    If you have signed in with Facebook before,  just click the Button.<BR>
    There's no need to enter your password. <BR>
    We will never spam your wall or post without your permission.
  </P>
  <a href="<%=omniauth_authorize_path(resource_name, 'facebook') %>" border=0 alt="Sign in with Facebook"><img src="/assets/facebook-login.png"></a>
 </div>
</div>

  <div class="control-group"> 
    <%= f.label :email, :class=>"control-label" %>
    <div class="controls">
      <%= f.email_field :email, :class=>"input-xlarge" %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :password, :class => "control-label"  %>
    <div class="controls">
      <%= f.password_field :password, :class=>"input-xlarge" %></div>
    </div>
  <div>

  <% if devise_mapping.rememberable? -%>
  <div class="control-group">
    <div class="controls">
      <label class="checkbox" for="user_remember_me">Remember me
      <%= f.check_box :remember_me %> 
      </label>
    </div>
  </div>
  <% end -%>

  <div class="control-group">
    <div class="controls">
      <%= f.submit "Sign in wth your Password", :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>

<%= render "devise/shared/links" %>
