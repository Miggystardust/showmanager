<% flash.each do |name, msg| %>
  <% amap = {"info" => "alert-info", "error" => "alert-error", "warning" => "", "notice" => "alert-success"}  %>
  <%= content_tag :div, msg, :class => "alert #{amap[name]} #{name}" if msg.is_a?(String) %>
<% end %>
